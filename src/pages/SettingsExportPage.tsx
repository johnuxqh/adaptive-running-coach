import { PlaceholderCard } from '../components/ui/PlaceholderCard';

export function SettingsExportPage() {
  return <PlaceholderCard title="Settings">App settings, export, and backup tools will appear here.</PlaceholderCard>;
}
